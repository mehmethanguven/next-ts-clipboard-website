export interface INavLink {
  title: string
  url: string
}

export interface ISocialLink {
  title: string
  imageUrl: string
  url: string
}

export interface IListItem {
  title: string
  desc: string
  imageUrl: string
}
